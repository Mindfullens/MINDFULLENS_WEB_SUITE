# Dependencies & build
node_modules
dist
dist-ssr
.vite
.eslintcache
coverage

# Env (kopiuj z .env.example)
.env
.env.*
!.env.example

# Tooling (lokalne cache; nie trzymaj w repo)
.turbo
.cache

# Logs & OS
*.log
npm-debug.log*
.DS_Store
._*

# Playwright / lokalne artefakty testów
test-results/

# Spike’i skryptów (minimalne DNG/PSD — wygenerowane lokalnie)
scripts/spike-dng-output/
scripts/spike-psd-output/
