# STOP — digest PSD: before + recipe (BT)

## Zakres

- **`filmLabExportManifestReaderExamples.js`** — scenariusze **`singlePsdWithBeforeAndRecipe`**, **`batchPsdWithBeforeAndRecipe`**: primary `.psd`, sidecar **before `image/jpeg`**, sidecar **`after_recipe.json`** (pełny zestaw flag `includeBeforeAfter` + `includeRecipeJson` przy `fileFormat: psd`).
- **`film-lab-export-perf-gates.mjs`** — asercja źródła reader examples uzupełniona o nazwy BT.
- **`docs/hme/EXPORT-PSD-DNG-SPIKE.md`** — lista scenariuszy PSD w digestcie.

## Testy

- `npm run test:film-lab-export-gates` — PASS.

## Następny krok (propozycja)

- ~~before_recipe przy PSD~~ — zrealizowane w **`stop-2026-05-01-bu.md`** (parity z runtime).

---
