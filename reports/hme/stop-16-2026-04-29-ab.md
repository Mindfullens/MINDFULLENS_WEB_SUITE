# STOP Etap 16 — 2026-04-29 (AB)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportManifestArtifact.js` — wspólny builder wierszy manifestu (kolejność pól, opcjonalny `sourceName` dla batch)
- `src/engine/useFilmLabEngine.js` — `manifestArtifacts.push` przez builder (eksport pojedynczy)
- `src/engine/batchProcessor.js` — `manifestEntries.push` przez builder (ZIP batch)
- `scripts/test-export-manifest-digest-reader-example.mjs` — regex na blok push zaktualizowany pod wywołanie buildera (regresja `before` → `sidecar`)

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Bez zmian zachowania eksportu; manifest JSON powinien pozostać semantycznie ten sam, z jednolitym źródłem prawdy dla pól wiersza artefaktu.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Matcher statyczny na źródło nadal zależy od obecności stringów variant w pliku.`
  - Mitigacja: `Test nadal scope’uje się do manifest push + assertuje artifactRole dla before.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Kontynuacja kontraktu eksportu PRO (kolejne pod-scenariusze lub twardy gate na pełny manifestProfile).`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
