# STOP Etap 16 — 2026-04-30 (X)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportManifestCanonicalRoles.js` — jedyna implementacja `canonicalFilmLabExportManifestArtifactRoleForVariant` (bez importów Node; bezpieczna dla bundla przeglądarki).
- `src/engine/filmLabExportManifestOptionalScenarioSemantics.js` — import mapera z pliku canonical + re-export dla istniejących importerów (skrypty Node).
- `src/engine/filmLabExportManifestArtifact.js` — na początku `buildFilmLabExportManifestArtifactRow`: jeśli `variant` nie mapuje się na rolę lub `artifactRole` ≠ kanon, `throw new Error` (twardy guard przy emisji manifestu).
- `scripts/film-lab-export-perf-gates.mjs` — gate na źródło `filmLabExportManifestCanonicalRoles.js` oraz na użycie mapera w builderze artefaktów.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- W ścieżce happy-path brak zmian; przy wewnętrznym błędzie (np. zły `artifactRole` dla `variant`) eksport może się zatrzymać z czytelnym wyjątkiem zamiast wyemitować manifest sprzeczny z kontraktem.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Nowy plik w grafie importów silnika (+1 moduł w chunku).`
  - Mitigacja: `Plik ~15 linii; koszt marginalny.`
- Decyzja: `Osobny moduł canonical roles zamiast importu z semantics (tam jest `node:assert`).`
  - Uzasadnienie: `Builder manifestu jest w bundlu przeglądarki — nie wolno ciągnąć Node assert.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Jeśli potrzeba: dodać krótki test Node tylko dla `filmLabExportManifestCanonicalRoles.js` (kilka przypadków granicznych) — opcjonalnie, bo semantyka + gate już pokrywają macierz.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
