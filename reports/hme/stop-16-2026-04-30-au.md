# STOP Etap 16 — 2026-04-30 (AU)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- **Jakość stratna (JPEG / WebP / AVIF):** `src/engine/filmLabExportFormats.js` — `FILM_LAB_EXPORT_LOSSY_FORMAT_IDS` + `defaultFilmLabExportLossyQualityForFormat`; `src/engine/filmLabExportEncode.js` — `opts.lossyQuality` (clamp 0.35–1), ścieżki canvas + `encodeFilmLabExportImageData`.
- **UI + prefs:** `src/FilmLabExportModal.jsx` — suwak 35–100% przy formatach lossy; zapis `lossyQuality` w `filmLab.exportModal.prefs.v1`; przekazanie do `exportImage` / `processBatch`.
- **Silnik:** `src/engine/useFilmLabEngine.js`, `src/engine/batchProcessor.js` — propagacja `lossyQuality`.
- **i18n / CSS:** `lossyQualityLabel`, `lossyQualityHint` (PL/EN); style sekcji jakości w `filmLabPage.css`.
- **Gate’y:** `scripts/film-lab-export-perf-gates.mjs`, `scripts/test-film-lab-export-canonical-roles.mjs`.
- `docs/hme/STAGES.md` — dopisek + link do tego raportu.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run test:i18n-parity`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Wpływ UX/produkt

- Użytkownik steruje kompresją dla JPEG/WebP/AVIF; PNG/TIFF bez zmian (bezstratne).

## Następny krok (propozycja)

- Roadmapa PSD/DNG lub osobne presety jakości per profil rozmiaru.

---
