# STOP Etap 16 — 2026-04-30 (BE)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- `src/FilmLabExportModal.jsx` — **`queryExportModalFocusables`** + `ref` na panelu `role="dialog"`; nasłuch `keydown` na dialogu: **Tab** z ostatniego focusable → pierwszy, **Shift+Tab** z pierwszego → ostatni (prosta pułapka fokusu); efekt zależy od `open` i `fileFormat` (suwak jakości pojawia się tylko dla formatów stratnych).
- `scripts/film-lab-export-perf-gates.mjs` — asercja obecności `queryExportModalFocusables`.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Wpływ UX / a11y

- Nawigacja klawiaturą nie „wypada” poza modal przy Tab (karty rozmiaru nadal nie są focusable — osobny temat).

## Następny krok (propozycja)

- `role="button"` + `tabIndex={0}` + Enter na kartach rozmiaru albo roadmapa PSD/DNG.

---
