# STOP Etap 16 — 2026-04-30 (AR)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- **WebP** jako format eksportu rastra (UI z `FILM_LAB_EXPORT_RASTER_FORMAT_IDS`, `canvas.toBlob('image/webp', 0.92)` w `encodeFilmLabExportCanvas` / `encodeFilmLabExportImageData`).
- `src/engine/filmLabExportFormats.js` — `webp` w kanonicznej liście ID.
- i18n: `filmLab.exportModal.format.webp` (PL/EN).
- Manifest: `filmLabExportManifestReaderExamples.js` — scenariusze opcjonalne `singleWebpNoRecipe`, `batchWebpNoRecipe`, `singleWebpWithBeforeNoRecipe`, `batchWebpWithBeforeNoRecipe`; `filmLabExportManifestOptionalScenarioSemantics.js` — token `Webp` → `fileFormat=webp`.
- `scripts/film-lab-export-perf-gates.mjs` — gałąź enkodera dla `webp`; `scripts/test-film-lab-export-canonical-roles.mjs` — normalizacja `WEBP`; `scripts/test-export-manifest-digest-reader-example.mjs` — `image/webp` ↔ `.webp` w optional scenarios.
- `docs/hme/STAGES.md` — lista formatów w podzbiorze zamkniętym + link do tego raportu.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Wpływ UX/produkt

- W modalu eksportu pojawia się pigułka **WebP**; ZIP batch i pojedynczy eksport używają `.webp` / `image/webp` zgodnie z ścieżką enkodera.

## Ryzyka

- Jak przy AVIF: starsze silniki bez wsparcia `toBlob` dla WebP mogą zwrócić błąd enkodowania — zachowanie spójne z innymi formatami opartymi o `toBlob`.

## Następny krok (propozycja)

- Roadmapa PSD/DNG albo kolejne formaty rastra wyłącznie po rozszerzeniu listy w `filmLabExportFormats.js` i przejściu gate’ów.

---
