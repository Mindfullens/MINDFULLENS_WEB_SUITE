# STOP Etap 16 — 2026-04-30 (Y)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `scripts/test-film-lab-export-canonical-roles.mjs` — krótki test Node dla `canonicalFilmLabExportManifestArtifactRoleForVariant` (mapowania zgodne z kontraktem + przypadki `null` / nieobsługiwany wariant).
- `package.json` — `test:film-lab-export-gates` uruchamia ten skrypt po perf-gate i teście digest reader.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS` (perf gate + digest reader + canonical roles)
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian runtime; CI / `npm test` łapią regresję w czystej funkcji mapującej role.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Redukcja macierzy w jednym małym pliku — nowe warianty wymagają aktualizacji testu.`
  - Mitigacja: `Zamierzone: fail fast przy zmianie kontraktu; semantyka `optionalScenarios` i tak wymaga spójności.`
- Decyzja: `Nie duplikować `test:film-lab-export-gates` w `ci` ręcznie — skrypt już rozszerzony.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Etap 16 PRO: uzupełnienie kryteriów `Done` w STAGES (np. manifest + formaty) albo pierwsza integracja użytkownika z pełnym przepływem eksportu — zależnie od priorytetu produktu.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
