# STOP Etap 16 — 2026-04-30 (AS)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- `src/FilmLabExportModal.jsx` — zapamiętywanie **formatu** i **trzech checkboxów** (maska PNG, before/after, recipe JSON) w `localStorage` pod kluczem `filmLab.exportModal.prefs.v1`; przy otwarciu modala wczytanie + normalizacja `fileFormat` przez `normalizeFilmLabExportFileFormat`; brak zapisu — przy pierwszym otwarciu domyślne wartości jak wcześniej (JPEG, maska/before wyłączone, recipe włączone).
- `scripts/film-lab-export-perf-gates.mjs` — asercja obecności klucza prefs + normalizacji formatu w modalu.
- `docs/hme/STAGES.md` — dopisek o localStorage w kryteriach Etapu 16 + link do tego raportu.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Wpływ UX/produkt

- Użytkownik nie traci ostatnio wybranego formatu i opcji eksportu między sesjami (o ile przeglądarka zezwala na `localStorage`).

## Ryzyka

- Prywatne okno / pełny magazyn: zapis jest cicho ignorowany — zachowanie jak wcześniej w ramach jednej sesji (stan React do czasu odświeżenia).

## Następny krok (propozycja)

- Roadmapa PSD/DNG albo dalsze ustawienia eksportu (np. jakość lossy) — osobna decyzja produktowa.

---
