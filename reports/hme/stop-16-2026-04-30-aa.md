# STOP Etap 16 — 2026-04-30 (AA)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `scripts/film-lab-export-perf-gates.mjs` — na starcie: `JSON.parse` `package.json` i `assert.equal` skryptu `test:film-lab-export-gates` względem zdefiniowanego łańcucha (perf gate → digest reader → canonical roles → manifest artifact row). Zapobiega przypadkowemu skróceniu `npm run test:film-lab-export-gates` w PR.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian produktu.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Lista `REQUIRED_FILM_LAB_EXPORT_GATE_CHAIN` w gate musi być ręcznie utrzymywana w parze z `package.json`.`
  - Mitigacja: `Zmiana łańcucha i tak wymaga edycji `package.json`; gate przypomina o aktualizacji tablicy w tym samym PR.`
- Decyzja: `Parsowanie JSON zamiast dopasowania surowego tekstu — mniej kruche przy formatowaniu pliku.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Opcjonalnie: wyciągnąć `REQUIRED_FILM_LAB_EXPORT_GATE_CHAIN` do małego modułu `scripts/film-lab-export-gate-chain.mjs` importowanego tylko z perf-gate (jedna stała) — tylko jeśli pojawi się trzeci konsument.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
