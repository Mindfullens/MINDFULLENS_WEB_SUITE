# STOP Etap 16 — 2026-04-30 (AY)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- `src/engine/filmLabExportManifestOptionalScenarioSemantics.js` — jedna ścieżka walidacji kluczy `export` w `optionalScenarios`: albo cztery klucze (`fileFormat` + trzy `include*`), albo pięć z posortowanym `lossyQuality`; przy wariancie pięciokluczowym — zakres `lossyQuality` i format stratny; token nazwy `Jpeg` → `fileFormat=jpeg`; `WithLossyQuality` w nazwie wymaga pola; wariant czterokluczowy nie może zawierać `lossyQuality`.
- `src/engine/filmLabExportManifestReaderExamples.js` — blueprint `singleJpegWithLossyQualityNoRecipe` (JPEG + `lossyQuality` 0.92) uzupełniający digest obok `singleAvifWithLossyQuality`.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Wpływ UX/produkt

- Brak zmian w UI; narzędzia czytające digest mają drugi przykład stratny (JPEG) z `export.lossyQuality`.

## Następny krok (propozycja)

- Opcjonalnie: batch blueprint z `lossyQuality` albo przywrócenie fokusu po zamknięciu modala eksportu.

---
