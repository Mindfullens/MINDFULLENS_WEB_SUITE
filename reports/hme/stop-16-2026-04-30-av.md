# STOP Etap 16 — 2026-04-30 (AV)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- `src/engine/filmLabExportFormats.js` — `manifestLossyQualityForFilmLabExport` (wartość do zapisu w JSON dla formatów lossy; PNG/TIFF → brak pola).
- `src/engine/useFilmLabEngine.js` — manifest pojedynczego eksportu: `export.lossyQuality` gdy dotyczy; `buildExportRecipeSnapshot` dopisuje `export.lossyQuality` w recipe (before / mask / after + batch `buildRecipeObject`).
- `src/engine/batchProcessor.js` — manifest ZIP: `export.lossyQuality` analogicznie.
- `src/engine/filmLabExportManifestReaderExamples.js` — notatka kontraktu dla readerów.
- `scripts/film-lab-export-perf-gates.mjs`, `scripts/test-film-lab-export-canonical-roles.mjs` — pokrycie helpera i ścieżek manifestu.
- `docs/hme/STAGES.md` — dopisek + link do tego raportu.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Wpływ UX/produkt

- Brak zmian w UI; narzędzia zewnętrzne i audyt widzą **faktyczną** jakość stratną zapisanej sesji eksportu.

## Następny krok (propozycja)

- Roadmapa PSD/DNG albo rozszerzenie optional scenarios digest o przykład z `lossyQuality` (jeśli potrzebny twardy blueprint).

---
