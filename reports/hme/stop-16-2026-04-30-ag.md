# STOP Etap 16 — 2026-04-30 (AG)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportEncode.js` — import `normalizeFilmLabExportFileFormat`; na wejściu `encodeFilmLabExportCanvas` i `encodeFilmLabExportImageData` format jest normalizowany (nieznany → `jpeg`), tak aby gałęzie `if (fileFormat === …)` zawsze widziały wartość z `FILM_LAB_EXPORT_RASTER_FORMAT_IDS`.
- `scripts/film-lab-export-perf-gates.mjs` — gate tekstowy na użycie `normalizeFilmLabExportFileFormat` w enkoderze.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Ścieżki już normalizowane w silniku pozostają bez zmian (idempotentna normalizacja); potencjalnie bezpieczniejsze zachowanie przy przyszłych wywołaniach enkodera spoza głównej ścieżki.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Minimalny koszt bundla — `filmLabExportFormats` już w grafie przez inne moduły silnika.`
- Decyzja: `Podwójna normalizacja (engine + encoder) akceptowalna jako tani guard w warstwie I/O.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Opcjonalnie: test jednostkowy Node z mockiem canvas — niski ROI; raczej E2E eksportu w przeglądarce gdy priorytet QA.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
