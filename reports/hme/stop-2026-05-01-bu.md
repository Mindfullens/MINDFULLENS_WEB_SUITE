# STOP — digest PSD: parity `before_recipe` (BU)

## Zakres

- **`filmLabExportManifestReaderExamples.js`** — scenariusze **`singlePsdWithBeforeAndRecipe`** i **`batchPsdWithBeforeAndRecipe`** uzupełnione o artefakt **`before_recipe`** (`application/json`), w kolejności jak w **`useFilmLabEngine.exportImage`** / **`batchProcessor`**: after (PSD) → before (JPEG) → **before_recipe** → after_recipe.
- **Notes** digestu — jedna linia opisująca ten łańcuch przy PSD.
- **`docs/hme/EXPORT-PSD-DNG-SPIKE.md`** — doprecyzowanie czterech plików sidecar przy pełnym before+recipe.

## Testy

- `npm run test:film-lab-export-gates` — PASS.

## Następny krok (propozycja)

- ~~PSD + maska (+ mask_recipe)~~ — zrealizowane w **`stop-2026-05-01-bv.md`**.

---
