# STOP Etap 16 — 2026-04-30 (BI)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Priorytet (ROI dla procesu)

- **Spójny wzorzec** jak przy presetach rozmiaru: widoczny podtytuł formatu + **`aria-labelledby`** na grupie pilli → mniej rozjazdów AT vs UI przy review i testach.
- **`aria-pressed`** na pillach → jednoznaczny stan wyboru formatu dla czytników (segment control).
- **`:focus-visible`** na `.export-format-pill` → ten sam standard co karty rozmiaru; mniej poprawek „braku obramowania fokusu” później.

## Zakres wdrożony

- `src/FilmLabExportModal.jsx` — `FILM_LAB_EXPORT_FORMAT_HEADING_ID`, grupa formatów z `aria-labelledby`, przyciski z `aria-pressed={fileFormat === id}`.
- `src/filmLabPage.css` — `:focus` / `:focus-visible` dla `.export-format-pill`.
- `scripts/film-lab-export-perf-gates.mjs` — asercje `aria-labelledby`, `aria-pressed`, CSS `:focus-visible`.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Następny krok (kolejność ważności)

1. **Checkboxy modala** (`:focus-visible` / spójny ring) — szybkie domknięcie fokusu w całym dialogu.
2. **Scenariusz manifestu** (np. edge batch) — jeśli pojawi się nowy wariant pipeline; inaczej utrzymanie reader examples.
3. Roadmapa **PSD/DNG** — tylko po decyzji produktowej (`NORTH-STAR`).

---
