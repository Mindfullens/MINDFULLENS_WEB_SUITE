# STOP Etap 16 — 2026-04-29 (U)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportManifestReaderExamples.js`

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS`
- Dodatkowe testy manualne: `N/A (reordering literałów export bez zmian wartości)`

## Wpływ UX/produkt (co użytkownik już widzi)

- Scenariusze `optionalScenarios` z `fileFormat` mają teraz spójny porządek kluczy w bloku `export`:
  - najpierw flagi `includeBeforeAfter`, `includeLocalMaskPng`, `includeRecipeJson`
  - na końcu `fileFormat`
- Ułatwia review diffów i utrzymanie spójności kontraktu bez zmiany semantyki.

## Ryzyka i decyzje architektoniczne

- Ryzyko: Kontrakty minimalnego scenariusza zakładają konkretny zestaw kluczy w bloku export — reorder nie zmienia zbioru kluczy, ale zmienia czytelną kolejność w repozytorium.
  - Mitigacja: `Test kontraktu optional scenarios nadal waliduje zbiór kluczy przez sorted equality (niezależnie od kolejności literału).`
- Decyzja: `Priorytet czytelności diffów: flagi include* zawsze przed fileFormat.`
  - Uzasadnienie: `Zmiany formatów nie rozwalają wizualnie całego bloku eksportu w diffie.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Opcjonalnie: ustalić jednolitą kolejność kluczy w minimalnych artefaktach (variant vs artifactRole jako pierwszy klucz) dla spójnych snippetów w dokumentacji integratorów.`
- Kryterium wejścia: `PASS lint/export-gates/build; aktualizacja testu sortedKeys dla artefaktów jeśli zmieniamy kontrakt kolejności.`

---
