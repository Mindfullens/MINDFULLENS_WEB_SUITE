# STOP — binarny SPIKE PSD (BO)

## Zakres

- **Zależności dev:** `ag-psd` (MIT), `canvas` (node-canvas — binaria platformowe).
- **Skrypt:** `scripts/spike-write-minimal-psd.mjs` — gradient RGB 64×64, jedna warstwa, zapis `scripts/spike-psd-output/spike-mindfullens-minimal.psd`.
- **npm:** `npm run spike:psd`.
- **`.gitignore`:** `scripts/spike-psd-output/` (output regenerowany lokalnie).
- **`docs/hme/EXPORT-PSD-DNG-SPIKE.md`:** §6 (ścieżka + komenda), §8 — checkboxy biblioteka/skrypt odhaczone.

## Weryfikacja automatyczna

- `writePsdBuffer` + roundtrip `readPsd` w Node potwierdzają strukturę (64×64, jedna warstwa).

## CI

- Domyślny łańcuch **`npm run test`** nie uruchamia SPIKE (native `canvas`).

## Następny krok

- Otwarcie wygenerowanego `.psd` w Photoshop/Affinity (checklista §8).
- Integracja z pipeline eksportu Film Lab — osobny epik / PR.

---
