# STOP Etap 16 — 2026-04-30 (S)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `scripts/film-lab-export-perf-gates.mjs` — domknięta walidacja canonical roles w examples:
  - `after` artifact => `artifactRole='primary'` (we wszystkich scenariuszach),
  - `mask` artifact w scenariuszach `WithMask` => `artifactRole='aux-mask'`,
  - istniejąca walidacja `before` => `artifactRole='sidecar'` pozostaje aktywna.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian runtime; examples i static gate spójnie egzekwują canonical role mapping dla kluczowych wariantów artefaktów.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Każdy świadomy wyjątek od canonical roles wymaga jawnej aktualizacji gate.`
  - Mitigacja: `To celowe; wymusza decyzję kontraktową zamiast cichego dryfu.` 
- Decyzja: `Utrzymać role mapping (`after`/`before`/`mask`) jako twardy kontrakt examples + gate.`
  - Uzasadnienie: `Spójność readerów zewnętrznych i mniejsze ryzyko regresji semantyki manifestu.` 

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Dodać w gate check, że każde scenario ma co najmniej jeden `after` artifact jako primary anchor (już implikowane, ale warto egzekwować explicite).`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
