# STOP Etap 16 — 2026-04-30 (BD)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- `src/FilmLabExportModal.jsx` — przy otwarciu modala zapamiętanie `document.activeElement` (gdy to sensowny `HTMLElement`); w cleanupie efektu (zamknięcie / `open` → `false`) **przywrócenie fokusu** na ten element, jeśli nadal jest w drzewie (`document.contains`); dalej `rAF` + fokus na przycisku ✕ po otwarciu.
- `scripts/film-lab-export-perf-gates.mjs` — asercja obecności logiki restore focus w modalu.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Wpływ UX / a11y

- Po zamknięciu eksportu fokus wraca do elementu, który modal otworzył (np. przycisk Eksport), zamiast zostawać na stracie.

## Następny krok (propozycja)

- Roadmapa PSD/DNG albo dalsze utwardzanie a11y (np. focus trap wewnątrz modala — większy zakres).

---
