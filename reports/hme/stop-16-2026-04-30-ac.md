# STOP Etap 16 — 2026-04-30 (AC)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `scripts/run-film-lab-export-gates.mjs` — przed każdym `spawnSync`: `existsSync` + `assert.ok` (brak pliku kroku → natychmiastowy komunikat); po udanym łańcuchu `console.log('PASS film-lab export gate chain')`.
- `scripts/film-lab-export-perf-gates.mjs` — lekki gate na źródło runnera (obecność `existsSync`).

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian produktu; przy złej ścieżce w `FILM_LAB_EXPORT_GATE_STEPS` CI dostaje czytelny błąd zamiast niejasnego ENOENT z child process.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Synchronyczny `existsSync` przy starcie runnera — koszt pomijalny przy czterech plikach.`
- Decyzja: `assert z `node:assert/strict` dla spójności z innymi skryptami gate.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Audyt pokrycia formatów eksportu (JPEG/PNG/TIFF/AVIF vs PSD/DNG w STAGES) — doprecyzować roadmap lub odhaczyć część jako „future“.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
