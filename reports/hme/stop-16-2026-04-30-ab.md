# STOP Etap 16 — 2026-04-30 (AB)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `scripts/film-lab-export-gate-steps.mjs` — jedyna lista `FILM_LAB_EXPORT_GATE_STEPS` (perf gate → digest reader → canonical roles → manifest artifact row).
- `scripts/run-film-lab-export-gates.mjs` — kolejne `spawnSync` Node dla każdego kroku, `stdio: 'inherit'`.
- `package.json` — `test:film-lab-export-gates` skraca się do `node scripts/run-film-lab-export-gates.mjs`.
- `scripts/film-lab-export-perf-gates.mjs` — walidacja `package.json` wobec delegacji do runnera (nie długiego `&&`); lekki regex na źródło `film-lab-export-gate-steps.mjs`.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS` (runner → cztery pod-skrypty)
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian produktu; prostsze utrzymanie łańcucha gate w jednym pliku.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Nowy skrypt startowy — osobny proces na krok (jak wcześniej łańcuch `&&`).`
  - Mitigacja: `Semantyka identyczna; wyjście kodem ≠0 przerywa jak dawniej.`
- Decyzja: `Lista kroków pozostaje w repo jako moduł ES, nie w ciągu npm — jedna edycja przy dodaniu testu.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Opcjonalnie: import `FILM_LAB_EXPORT_GATE_STEPS` w runnerze + dynamiczny `import()` zamiast spawn — tylko jeśli chcemy jeden proces Node (większy refaktor).`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
