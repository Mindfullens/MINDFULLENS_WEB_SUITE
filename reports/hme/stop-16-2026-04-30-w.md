# STOP Etap 16 — 2026-04-30 (W)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportManifestOptionalScenarioSemantics.js`:
  - nowa funkcja `canonicalFilmLabExportManifestArtifactRoleForVariant` (`after`→`primary`, `mask`→`aux-mask`, `before` lub wariant zawierający `recipe`→`sidecar`, inaczej `null`);
  - wcześniejsza walidacja `Array.isArray(artifacts)`;
  - pojedyncza ścieżka weryfikacji `artifactRole` dla każdego minimalnego wiersza (zamiast osobnych pętli dla before/mask/after);
  - usunięte zduplikowane pętle ról powiązane z sufiksami `WithBefore` / `WithMask` (nadal egzekwowane są flagi i obecność wariantów).
- `scripts/test-export-manifest-digest-reader-example.mjs` — import kanonicznego mappera dla `assertArtifactRowBlueprintConsistency`; usunięte `assertVariantRoleInvariants` (pokryte przez `assertFilmLabExportOptionalScenariosSemantics`).
- `scripts/film-lab-export-perf-gates.mjs` — lekki gate na źródle modułu semantics (obecność eksportu `canonicalFilmLabExportManifestArtifactRoleForVariant`).

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian runtime; jedna funkcja źródłowa dla mapowania variant→role w kontrakcie reader + blueprint `artifactRow*`.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Silnik runtime (`buildFilmLabExportManifestArtifactRow`) nie importuje jeszcze tej funkcji — nadal możliwy teoretyczny rozjazd jeśli ktoś zmieni tylko emitter.`
  - Mitigacja: `Gate + semantics + test blueprint; kolejny krok mógłby podpiąć asercję w builderze lub współdzielić stałe — poza tym STOP.`
- Decyzja: `Traktować `canonicalFilmLabExportManifestArtifactRoleForVariant` jako kontrakt dokumentacyjny / walidacyjny, nie wymuszać go w hot path bez potrzeby.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Opcjonalnie: użyć `canonicalFilmLabExportManifestArtifactRoleForVariant` w `buildFilmLabExportManifestArtifactRow` (dev-only assert lub produkcyjny guard) — tylko jeśli chcemy twardo związać emitter z kontraktem.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
