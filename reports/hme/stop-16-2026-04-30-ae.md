# STOP Etap 16 — 2026-04-30 (AE)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportFormats.js` — `FILM_LAB_EXPORT_RASTER_FORMAT_IDS`, `FILM_LAB_EXPORT_RASTER_FORMAT_SET`, `normalizeFilmLabExportFileFormat` (jedna prawda dla listy formatów rastra).
- `src/FilmLabExportModal.jsx` — pill formatów z importu stałej zamiast lokalnej tablicy.
- `src/engine/useFilmLabEngine.js`, `src/engine/batchProcessor.js` — normalizacja `fileFormat` przez `normalizeFilmLabExportFileFormat`.
- `src/engine/filmLabExportManifestOptionalScenarioSemantics.js` — whitelist `fileFormat` w optional scenarios zsynchronizowana z tym samym zbiorem.
- `scripts/film-lab-export-perf-gates.mjs` — gate na źródło `filmLabExportFormats.js`.
- `scripts/test-film-lab-export-canonical-roles.mjs` — krótkie asercje dla normalizacji (case + fallback).
- `docs/hme/STAGES.md` — dopisany `filmLabExportFormats.js` w kluczowych plikach Etapu 16.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmiany zachowania — ta sama domyślna normalizacja `jpeg` i ta sama lista formatów, tylko jeden moduł źródłowy.

## Ryzyka i decyzje architektoniczne

- Decyzja: `Set + freeze(ids) zamiast wyłącznie tablicy — szybkie `.has` w walidacji manifestu.`
- Ryzyko: `Każdy nowy format rastra wymaga aktualizacji `filmLabExportFormats.js` + enkodera — zamierzona pojedyncza stacja kontrolna.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Rozważyć import `FILM_LAB_EXPORT_RASTER_FORMAT_IDS` w JSDoc/`filmLabExportEncode.js` jako odniesienie dokumentacyjne (bez zmiany logiki switch).`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
