# STOP Etap 16 — 2026-04-30 (AK)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportManifestReaderExamples.js` — uwaga w `notes`: `optionalScenarios.export.fileFormat` musi być zgodny z `FILM_LAB_EXPORT_RASTER_FORMAT_IDS` (`filmLabExportFormats.js`).
- `scripts/test-film-lab-export-canonical-roles.mjs` — `FILM_LAB_EXPORT_RASTER_FORMAT_IDS.length === FILM_LAB_EXPORT_RASTER_FORMAT_SET.size` (wykrywa duplikaty bez osobnej pętli).
- `scripts/film-lab-export-perf-gates.mjs` — gate tekstowy na odniesienie do `filmLabExportFormats` / `FILM_LAB_EXPORT_RASTER_FORMAT_IDS` w `readerExamples`.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian zachowania; czytelniejszy kontrakt zewnętrznych readerów manifestu względem listy formatów.

## Ryzyka i decyzje architektoniczne

- Decyzja: `Nie importować `filmLabExportFormats` w `readerExamples` — tylko notatka tekstowa, aby uniknąć ryzyka cykli i utrzymać plik jako statyczne blueprinty.`
- Ryzyko: `Duplikacja semantyki (notatka + semantyka Node) — mitigacja: perf-gate łączy string w pliku.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Etap 16 PRO: ewentualne „zamknięcie” podzbioru (manifest + rastry) jako podkryterium `Done` w STAGES, reszta roadmapy osobno.`
- Kryterium wejścia: `decyzja produktowa + PASS lint/export-gates/build.`

---
