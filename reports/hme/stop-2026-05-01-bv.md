# STOP — digest PSD: maska + mask_recipe (BV)

## Zakres

- **`filmLabExportManifestReaderExamples.js`** — scenariusze **`singlePsdWithMaskNoRecipe`**, **`batchPsdWithMaskNoRecipe`** (primary `.psd` + `mask.png`); **`singlePsdWithMaskAndRecipe`**, **`batchPsdWithMaskAndRecipe`** (+ `mask_recipe.json`, `after_recipe.json`) w kolejności jak **`useFilmLabEngine.exportImage`** / **`batchProcessor`** (blok maski przed `after_recipe`).
- **Notes** digestu — krótka adnotacja o PNG maski przy PSD i recipe.
- **`filmLabExportManifestOptionalScenarioSemantics.js`** — reguła **`AndRecipe`** w nazwie scenariusza wymusza `includeRecipeJson` + obecność wariantów recipe (uzupełnienie wzorca `*WithBeforeAndRecipe` / `*WithMaskAndRecipe`, bo podciąg `WithRecipe` ich nie łapie).
- **`film-lab-export-perf-gates.mjs`**, **`docs/hme/EXPORT-PSD-DNG-SPIKE.md`**, **`docs/hme/STAGES.md`**.

## Testy

- `npm run test:film-lab-export-gates` — PASS.

## Następny krok (propozycja)

- ~~Full stack PSD + before + mask + recipe~~ — zrealizowane w **`stop-2026-05-01-bw.md`** (`*WithBeforeWithMaskAndRecipe`).

---
