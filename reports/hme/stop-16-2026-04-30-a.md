# STOP Etap 16 — 2026-04-30 (A)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportManifestHelpers.js` — dodany `buildFilmLabExportManifestRootBase(...)` scalający wspólne pola root manifestu (schema/profile/refs/compat/generator/build/capabilities/counts/artifacts).
- `src/engine/useFilmLabEngine.js` — single export buduje root manifestu przez wspólny helper i dokleja tylko część specyficzną (`film`, `export`).
- `src/engine/batchProcessor.js` — batch export używa tego samego helpera root + własnego bloku `export` (`totalSources`, `exportedSources`).
- `scripts/film-lab-export-perf-gates.mjs` — gate sprawdza użycie `buildFilmLabExportManifestRootBase(...)` w obu emitterach.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian w outputach funkcjonalnych eksportu; manifest root jest składany jednolicie dla single i batch.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Zmiana helpera root wpływa jednocześnie na single i batch.`
  - Mitigacja: `Static gates + test digest reader examples + pełny lint/build.`
- Decyzja: `Utrzymać jeden builder root manifestu zamiast duplikacji literalnych obiektów.`
  - Uzasadnienie: `Mniejsza szansa rozjazdu kontraktu i prostsze kolejne zmiany Etapu 16.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Konsolidacja pozostałych kontraktów eksportu PRO (np. specyficzne reguły dla kolejnych formatów/PSD layers) lub rozszerzenie gate o stricte root-shape invariants.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
