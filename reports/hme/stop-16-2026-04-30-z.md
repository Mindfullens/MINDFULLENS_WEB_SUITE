# STOP Etap 16 — 2026-04-30 (Z)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `scripts/test-film-lab-export-manifest-artifact-row.mjs` — test integracyjny `buildFilmLabExportManifestArtifactRow`:
  - happy path `after` + `primary` + stub SHA-256;
  - `assert.rejects` gdy `artifactRole` nie zgadza się z kanonem;
  - `assert.rejects` dla nieznanego `variant`;
  - wiersz batch z `sourceName` — pierwszy klucz obiektu to `sourceName` (stabilne diffy JSON).
- `package.json` — `test:film-lab-export-gates` dołącza ten skrypt na końcu łańcucha.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS` (perf gate + digest + canonical roles + artifact row)
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian UI; regresja w builderze wiersza manifestu (zła rola / wariant) jest łapana w CI przed regresem w polu `manifest.json`.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Stub `sha256HexFromBytes` nie weryfikuje prawdziwego hasha pliku.`
  - Mitigacja: `Cel testu to guard ról, nie kryptografii; digest manifestu testowany osobno.`
- Decyzja: `Osobny skrypt zamiast rozszerzania testu canonical roles — czytelny podział odpowiedzialności.`

## Następny etap (1 krok dalej)

- Następny etap: `16` lub `18` (release gates)
- Zakres startowy: `Rozszerzyć `film-lab-export-perf-gates` o lekki regex na `test-film-lab-export-manifest-artifact-row.mjs` w `package.json` (opcjonalnie) albo przejść do twardego kryterium `Done` dla eksportu PRO w `STAGES.md` gdy wszystkie formaty będą domknięte.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
