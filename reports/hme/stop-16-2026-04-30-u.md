# STOP Etap 16 — 2026-04-30 (U)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportManifestOptionalScenarioSemantics.js` — nowy moduł z `assertFilmLabExportOptionalScenariosSemantics` (jeden zestaw reguł: prefiks `single`/`batch`, sufiksy `WithRecipe`/`NoRecipe`/`WithBefore`/`WithMask`, tokeny `Png`/`Avif`/`Tiff`, whitelist `fileFormat`, wyjątek `singleWithBeforeMaskRecipe` / `batchWithBeforeMaskRecipe`, role `before`/`after`/`mask`, obowiązkowy co najmniej jeden `after`).
- `scripts/film-lab-export-perf-gates.mjs` — pętla źródłowa (nazwa scenariusza w pliku `.js`) + wywołanie wspólnej asercji zamiast zduplikowanego bloku.
- `scripts/test-export-manifest-digest-reader-example.mjs` — wczesne wywołanie tej samej asercji po walidacji `schemaHint`.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian runtime; kontrakt `optionalScenarios` jest egzekwowany w jednym miejscu, mniejsze ryzyko rozjazdu gate vs test.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Moduł w `src/engine` importuje `node:assert` — nie jest importowany z kodu przeglądarkowego (tylko skrypty Node).`
  - Mitigacja: `Tylko `scripts/*` importują ten plik; bundler Vite go nie wciąga.`
- Decyzja: `API asercji (`assert*`) zamiast zwracania listy błędów — spójne z resztą gate/testów.`
  - Uzasadnienie: `Mniej boilerplateu; komunikaty `assert` są wystarczająco czytelne w CI.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Opcjonalnie: dodać perf-gate regex, że `filmLabExportManifestReaderExamples.js` nie duplikuje reguł inline (np. wzmianka importu semantics) — niski priorytet jeśli import w gate już gwarantuje moduł.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
