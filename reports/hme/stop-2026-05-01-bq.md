# STOP — pill PSD w modalu + batch (BQ)

## Zakres

- **`FILM_LAB_EXPORT_MODAL_FORMAT_IDS`** — raster + **`psd`**; modal eksportu renderuje pstryki z tej listy.
- **i18n** PL/EN: `filmLab.exportModal.format.psd`.
- **`test-film-lab-export-format-i18n.mjs`** — waliduje etykiety dla wszystkich ID modala (w tym PSD).
- **`batchProcessor`** — przy `fileFormat: psd` ten sam kodowanie PSD co single (ostrzenie + `encodeFilmLabExportPsdFromCanvas`); sidecary before/mask recipe z **`rasterFormatForSidecars`** (`jpeg`).
- **`film-lab-export-perf-gates.mjs`** — zaktualizowane asercje modala i batcha.
- **`docs/hme/NORTH-STAR.md`**, **`EXPORT-PSD-DNG-SPIKE.md`** — UI PSD opisane.

## Testy

- `npm run lint`, `npm run test:i18n-parity`, `npm run test:film-lab-export-gates`, `npm run build` — PASS.

## Uwaga

- **Digest manifestu** (`optionalScenarios` w reader examples) nadal dotyczy wyłącznie rastra z `FILM_LAB_EXPORT_RASTER_FORMAT_IDS`; `export.fileFormat: psd` w żywym manifeście jest dozwolone poza tą macierzą.

---
