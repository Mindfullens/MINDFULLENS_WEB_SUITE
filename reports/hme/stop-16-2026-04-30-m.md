# STOP Etap 16 — 2026-04-30 (M)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `scripts/film-lab-export-perf-gates.mjs` — rozszerzenie data-driven walidacji `optionalScenarios`:
  - whitelist `export.fileFormat` (`jpeg`, `png`, `tiff`, `avif`) dla scenariuszy, które podają format,
  - walidacja `mode` (`single|batch`) dla każdego scenariusza,
  - utrzymanie reguły specjalnej: `singleWithBeforeMaskRecipe` / `batchWithBeforeMaskRecipe` nie deklarują `fileFormat` w minimalnym `export` bloku.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian runtime; static gate szybciej wyłapie niepoprawne wartości `fileFormat` i niespójne shape scenariuszy.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Jeśli dojdzie nowy format eksportu, whitelist gate wymaga jawnej aktualizacji.`
  - Mitigacja: `To celowy fail-fast dla kontraktu examples/gate.` 
- Decyzja: `Trzymać whitelist formatów na poziomie static gate.`
  - Uzasadnienie: `Chroni przed cichymi literówkami i przypadkowym dryfem kontraktu.` 

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Dodać gate na zgodność `mode` z prefixem nazwy scenariusza (`single*`/`batch*`) dla pełnej spójności data-driven matrixu.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
