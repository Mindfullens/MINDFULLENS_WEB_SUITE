# STOP Etap 16 — 2026-04-30 (AX)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- **`src/engine/filmLabExportManifestReaderExamples.js`** — scenariusz opcjonalny `singleAvifWithLossyQuality`: minimalny manifest digest z `export.fileFormat: 'avif'` oraz `export.lossyQuality: 0.88`; dopisek w `notes` o kontrakcie kluczy eksportu z `lossyQuality`.
- **`src/engine/filmLabExportManifestOptionalScenarioSemantics.js`** — gałąź walidacji: gdy w bloku `export` jest `lossyQuality`, wymagany zestaw posortowanych kluczy `fileFormat` + trzy flagi `include*` + `lossyQuality`; wartość numeryczna w `[0.35, 1]`; `fileFormat` musi należeć do formatów stratnych (`jpeg` / `webp` / `avif`); nazwa scenariusza zawierająca `WithLossyQuality` wymaga obecności pola.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Wpływ UX/produkt

- Brak zmian UI; zewnętrzne narzędzia walidujące digest mają twardy przykład z `export.lossyQuality` zgodny z emiterami manifestu.

## Następny krok (propozycja)

- Opcjonalnie: scenariusz batch z `lossyQuality`, przywrócenie fokusu po zamknięciu modala, albo roadmapa PSD/DNG.

---
