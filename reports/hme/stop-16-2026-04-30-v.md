# STOP Etap 16 — 2026-04-30 (V)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `src/engine/filmLabExportManifestOptionalScenarioSemantics.js` — rozszerzenie `assertFilmLabExportOptionalScenariosSemantics`:
  - blok `export`: dokładny zestaw kluczy (po sortowaniu alfabetycznym), z rozgałęzieniem `singleWithBeforeMaskRecipe` / `batchWithBeforeMaskRecipe` (tylko `include*`) vs pozostałe scenariusze (`fileFormat` + `include*`);
  - każdy artifact w scenariuszu: kanoniczna kolejność `variant` → `artifactRole` → `fileName` → `mimeType`, brak kluczy runtime (`byteLength`, `sha256`, itd.);
  - eksport stałej `FILM_LAB_EXPORT_MANIFEST_MINIMAL_OPTIONAL_ARTIFACT_KEYS` dla ewentualnych odniesień z innych modułów.
- `scripts/test-export-manifest-digest-reader-example.mjs` — usunięty zduplikowany `assertOptionalScenarioMinimalContract` i powiązane stałe (reguły pokrywa wyłącznie moduł semantics).

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian runtime; minimalny kontrakt `optionalScenarios` jest teraz egzekwowany także w ścieżce perf-gate (nie tylko w długim teście digest).

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Sortowanie kluczy `export` zakłada, że nowe pola nie wejdą do minimalnego bloku bez aktualizacji modułu.`
  - Mitigacja: `To zamierzone — reader contract jest zamknięty na te cztery / trzy pola w zależności od scenariusza.`
- Decyzja: `Trzymać „minimal contract” przy semantyce nazw, nie w osobnym pliku testowym.`
  - Uzasadnienie: `Jeden import w gate + test; brak rozjazdu kolejności asercji.`

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Rozważyć przeniesienie mapowania variant → artifactRole (obecnie `expectedRoleForVariant` w teście) do wspólnego modułu tylko jeśli zacznie być używane poza testem — inaczej zostawić w teście jako „mirror” kontraktu.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
