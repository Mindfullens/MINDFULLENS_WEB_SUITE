# STOP Etap 16 — 2026-04-30 (BJ)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony

- `src/filmLabPage.css` — dla wierszy **`export-modal-mask-row`** (checkbox + opis): **`input:focus { outline: none }`** oraz **`export-modal-mask-row:has(input:focus-visible)`** z konturem `var(--accent)` i `border-radius`, żeby przy Tabie było widać fokus na całym klikalnym wierszu (maska PNG / before-after / recipe JSON).

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)

## Uwaga techniczna

- Użyty selektor **`:has()`** — wspierany w aktualnych przeglądarkach (modal Film Lab docelowo desktop/modern).

## Następny krok (z kolejki)

- Scenariusz manifestu tylko przy nowym wariantcie pipeline; inaczej roadmapa PSD/DNG po decyzji produktowej.

---
