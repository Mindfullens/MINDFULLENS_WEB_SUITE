# STOP — digest PSD: recipe + before (BS)

## Zakres

- **`filmLabExportManifestReaderExamples.js`** — scenariusze:
  - **`singlePsdWithRecipe`**, **`batchPsdWithRecipe`** — primary `.psd` + `after_recipe.json`;
  - **`singlePsdWithBeforeNoRecipe`**, **`batchPsdWithBeforeNoRecipe`** — primary `.psd` + **before `image/jpeg`** (zgodnie z `rasterFf` w `useFilmLabEngine` / `batchProcessor`).
- **Notes** digestu — jawna adnotacja o JPEG dla before przy primary PSD.
- **`film-lab-export-perf-gates.mjs`** — rozszerzona asercja listy nazw scenariuszy PSD.

## Testy

- `npm run test:film-lab-export-gates` (w tym digest reader example) — PASS.

## Następny krok (propozycja)

- Opcjonalnie: **PSD + before + recipe** (trzy artefakty) jeśli produkt utrwala ten zestaw w jednym eksporcie.

---
