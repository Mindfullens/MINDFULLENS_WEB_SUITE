# STOP Etap 16 — 2026-04-30 (R)

## Etap

- Numer: `16`
- Nazwa: `Eksport v1 PRO`
- Status: `GO`

## Zakres wdrożony (pliki/moduły)

- `scripts/film-lab-export-perf-gates.mjs` — rozszerzona semantyka `WithBefore`:
  - nadal wymaga flagi `includeBeforeAfter=true` i obecności wariantu `before`,
  - dodatkowo wymaga, by każdy artifact `variant=before` miał `artifactRole='sidecar'`.
- Dzięki temu kontrakt migration note (`before` identyfikujemy po variant, canonical role = sidecar) jest chroniony także na poziomie examples/perf-gate.

## Testy i wynik

- `npm run lint`: `PASS`
- `npm run test:film-lab-export-gates`: `PASS`
- `npm run build`: `PASS` (w tym `test:dist-outputs`)
- Dodatkowe testy manualne: `N/A`

## Wpływ UX/produkt (co użytkownik już widzi)

- Brak zmian runtime; mocniejszy kontrakt examples dla wariantu `before` i mniejsze ryzyko regresji roli artefaktu.

## Ryzyka i decyzje architektoniczne

- Ryzyko: `Ewentualny przyszły wyjątek od canonical role=sidecar dla variant=before wymaga jawnej zmiany gate.`
  - Mitigacja: `To zamierzone — wyjątek powinien być świadomą decyzją i migracją kontraktu.` 
- Decyzja: `Egzekwować canonical `before -> sidecar` również w static gate dla optionalScenarios.`
  - Uzasadnienie: `Spójność z readerExamples notes i testem regresji runtime source.` 

## Następny etap (1 krok dalej)

- Następny etap: `16`
- Zakres startowy: `Dodać analogiczną walidację canonical role dla `after` (`primary`) i `mask` (`aux-mask`) bezpośrednio w static gate.`
- Kryterium wejścia: `PASS lint/export-gates/build.`

---
