# https://docs.netlify.com/configure-builds/file-based-configuration/
# Nagłówki HTTP: public/_headers (trafia do dist/_headers — jedno źródło z Cloudflare Pages)
[build]
  command = "npm run build"
  publish = "dist"
